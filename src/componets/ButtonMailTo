import React from "react";

const ButtonMailTo = ({ mailto, imgSrc }) => {
    return (
        <a
            to='#'
            onClick={(e) => {
                window.location.href = mailto;
                e.preventDefault();
            }}
        >
            <img src={imgSrc} alt="" />
        </a>
    );
};

export default ButtonMailTo;